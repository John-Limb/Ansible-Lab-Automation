apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: podinfo
      version: '>=5.0.0'
      sourceRef:
        name: podinfo
        kind: HelmRepository
        namespace: flux-system
      interval: 1m